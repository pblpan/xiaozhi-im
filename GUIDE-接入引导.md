# 接入引导：把「网络运维工具箱」的告警推进 IM 群

> **目标**：工具箱每 60 秒巡检一轮，一旦发现设备离线 / CPU·内存越限 / 设备恢复，
> IM 群里立刻弹一张卡片，值班的人扫一眼就知道轻重。
> **耗时**：约 10 分钟。**不用写代码，不用重启任何服务。**
> 本文以服务端 v0.10.1 + 工具箱 v1.1 为准，每条结论都在本机实测过（`server/test/netops_hook_test.js`，37 项断言全绿）。

---

## 零、先别急着点：你截图里的两个页面不是一回事

这是最容易搞混的地方，先分清再动手：

| 你想干的事 | 该用哪个页面 | 说明 |
|---|---|---|
| 让**网络运维工具箱**把告警发到 IM 群 | 集成对接 → **入站推送（外部 → 群）** | 外部系统「往里推消息」的唯一入口 |
| 改 IM 客户端的服务器地址 / 功能开关 / 强制升级 / 发公告 | **客户端配置** | 它只对**小智 IM 自己的客户端**生效 |

一句话记住：

> **入站推送是「外面往群里推」，客户端配置是「服务端往客户端下发设置」。两者没有任何交集。**

所以你要的这个需求，**只需要用到「入站推送」，跟「客户端配置」完全无关。**
（客户端配置真正该用的场景见第六节。）

---

## 一、IM 后台：先造一个「发言人」

机器人是系统在群里的身份。消息以它的名义发出，群里能一眼看出是系统推的，不会冒充真人。

1. 浏览器打开 `http://192.168.31.44:3602/admin`，用管理员账号登录
2. 左侧菜单点 **集成对接**
3. 切到 **机器人** 标签 → 点 **新建机器人**
4. 第 1 步「名称」填 `网维告警`（这个名字会显示在群成员列表里，建议起得一眼能认出来）→ 下一步
5. 第 2 步「加入会话」**可以先不选** —— 下一步建推送地址时会自动把机器人拉进目标群

> 建完你会在列表里看到它带一个「机器人」标签。它**不能登录**，只能通过 Webhook / API 令牌发言，所以不用担心有人拿它当账号用。

---

## 二、IM 后台：建推送地址（就是你截图里那个对话框）

1. 还是 **集成对接** 页，切到 **入站推送（外部 → 群）** 标签
2. 点 **新建推送地址**，弹出你截图里那个两步骤的对话框：

**第 1 步 · 用途**

| 字段 | 填什么 |
|---|---|
| 用途 | `网维告警`（只是个备注名，方便你以后认出来是哪条） |
| 机器人 | 下拉选第 1 步建的 `网维告警（bot_xxxxxx）` |

点 **下一步**。

**第 2 步 · 目标**

| 字段 | 填什么 |
|---|---|
| 推到会话 | 选目标群，例如「群｜网络运维告警群（3人）」 |
| 签名校验 | ⚠️ **保持关闭**（原因见第五节第 3 条） |

点 **创建**。

3. 回到列表，**「推送地址」那一列右边有个「复制」按钮** —— 点它。你会得到形如：

```
http://192.168.31.44:3602/api/hooks/incoming/86ef7e0987568a99e86aef8455f728c4
```

> 地址里那串 token **就是凭证**，谁拿到谁就能往群里发消息。别贴到公开仓库、别发群里。

4. **先自检再往下走**：把上面这个地址直接粘到浏览器地址栏打开（GET）。
   返回一段 `{"ok":true,"hook":"网维告警","bot":"网维告警","conversationId":1,...}` 就说明地址通、机器人在群里、目标会话对。
   返回 `404 {"error":"hook not found"}` 就是地址被停用或删了。

5. **再点一次后台的「试推」按钮**，群里应该立刻出现一张卡片 —— 这一步过了，IM 这边就彻底 OK 了。

---

## 三、工具箱那边：把它接上

工具箱内置了通用 webhook 推送，正好能用：

1. 打开工具箱 `http://192.168.31.44:5000`，登录
2. 进 **告警中心** → 切到 **通知配置** 标签 → 点 **新增通道**
3. 按下表填：

| 字段 | 填什么 |
|---|---|
| 名称 | `小智 IM 告警群` |
| 类型 | **通用 Webhook** ← 关键，别选钉钉/企微/飞书 |
| Webhook 地址 | 粘贴第二步复制的那串 `http://192.168.31.44:3602/api/hooks/incoming/...` |
| 密钥 / secret | ⚠️ **留空**（填了 IM 会返回 401，见第五节第 3 条） |
| 最低级别 | 想安静点就选 `critical`（只推严重）；想全都要就选 `warning` |
| @成员 | 留空。想 @所有人时见第五条第 6 条 |

4. 先点 **测试推送**（不要急着勾启用）。工具箱会发一条固定内容的测试告警：
   - 标题：`推送测试`
   - 级别：`warning`（所以卡片是**橙色**）
   - 正文：`【警告】推送测试 / 设备：测试设备（127.0.0.1）/ ...`
   - 消息：`这是一条来自「网络运维自用工具箱」的测试消息，收到即说明通道配置正确。`
5. 群里出现了这张橙色卡片 → 回到通道列表，**勾上「启用」**。收工。

之后工具箱每 60 秒巡检一次，触发告警就自动推；设备恢复时也会推一条「已恢复」。

---

## 四、群里实际会收到什么

工具箱的通用 Webhook 发的是一坨业务 JSON，IM 会自动把它"擦洗"成一张卡片。
**实测**（下面是真实落库的内容）：

```json
{
  "title": "设备离线",
  "text": "【严重】设备离线\n设备：核心交换机-1F（192.168.31.2）\n来源：ping_monitor\n时间：2026-09-13 10:30:00\n详情：Ping 超时，连续 3 次无响应",
  "fields": [],
  "color": "red",
  "footer": "",
  "url": ""
}
```

在群里渲染出来就是：**红色卡片** + 加粗标题「设备离线」+ 五行正文（设备名 / IP / 来源 / 时间 / 详情都在，一个都没丢）。

颜色是按工具箱的告警级别**自动**推出来的，不用你去配：

| 工具箱级别 | 卡片颜色 | 长什么样 |
|---|---|---|
| `critical`（严重） | 🔴 red | 一眼看出是事故 |
| `major`（主要） | 🔴 red | 同样按严重处理 |
| `warning`（警告） | 🟠 orange | 需要留意 |
| `info`（提示） | 🔵 blue | 普通通知 |
| 认不出的级别 | 🔵 blue | 兜底，不会报错 |

---

## 五、踩坑清单（都是我实测撞出来的，不是猜的）

**1. 地址前缀少一个 `/api` 就是 404**
必须是 `/api/hooks/incoming/<token>`。漏掉 `/api` 返回 `404 {"error":"not found"}`，
这个错误长得像"服务没启动"，很容易往错方向排查。**用后台的「复制」按钮，别手打。**

**2. 别照抄钉钉 / 企微 / 飞书的报文格式**
如果你手上已有别的系统的推送代码，改个地址就发过来，发的是：
```json
{"msgtype":"text","text":{"content":"你好"}}
```
这种形式**现在会被明确拒绝**并告诉你哪儿错了：
```
400  text 必须是字符串，收到的是对象。
     如果你在对接钉钉/企微/飞书的报文格式（如 {"msgtype":"text","text":{"content":"..."}}），
     请把纯文本提到顶层：{"text":"..."} —— IM 不解析各家的嵌套结构。
```
> 顺带说一句：这个"明确拒绝"是本轮新修的。**改之前的行为是返回 200 成功，
> 但群里出现一条内容是 `[object Object]` 的消息** —— 发送方以为通了、接收方看到乱码，
> 属于最难排查的一类假成功。已经做过变异测试确认新的报错断言真的会红。

**3. 「签名校验」开着，工具箱就推不进来**
工具箱的 secret 生成的是 `X-Netops-Token` 请求头，而 IM 要的是
`X-Xiaozhi-Signature: sha256=<HMAC-SHA256(原始请求体, secret)>`。两边算的不是一个东西。
- 所以：**建推送地址时「签名校验」保持关闭**，工具箱那边「密钥」也留空。
- 安全性靠什么保证？靠"地址即凭证" + 后台可随时「停用」或「删除」，以及 URL 里的 token 不泄露。
- 如果你确实需要签名（比如要走公网），得在中间加一层转发做签名 —— 见第七节。

**4. 返回 429 = 推得太快，每分钟上限 120 条**
通常说明工具箱在刷屏。去工具箱侧看「推送日志」，确认是不是某个设备反复告警；
参考它的冷却机制和「最低级别」设置。

**5. 推送地址显示「未使用」**
说明工具箱一次都没发成功过。按顺序查：① 地址是不是手打错了；② 工具箱的「推送日志」里
那条记录写的 HTTP 状态码；③ 工具箱所在容器能不能访问 `192.168.31.44:3602`
（后台「复制」出来的地址用的是你访问管理台的那个主机名，如果管理台是走域名开的，
粘过去之前先确认工具箱能不能解析那个域名）。

**6. 想 @所有人**
IM 的入站接口支持 `"mentions": ["all"]`，但工具箱的通用 Webhook **不发这个字段**
（它只在钉钉/企微通道里处理 @）。所以要么加中间层（第七节），
要么就接受"卡片上没有 @"。值班群建议不加，告警卡片本身就是提醒。

**7. 告警恢复会再推一条**
工具箱在设备恢复时会发「已恢复」通知，级别通常是 `info` → 卡片是蓝色的。
如果嫌吵，把「最低级别」设成 `critical` 就只剩严重告警了。

---

## 六、那「客户端配置」到底是干什么的？

你截图里第三张那个页面。它解决的是**另一个问题**：服务端要统一管所有 IM 客户端的行为。
它**收不到也发不出消息**，跟第三方系统对接没有任何关系。

| 页面上的字段 | 真正的作用 |
|---|---|
| 服务器地址候选 | 客户端连不上时按顺序挨个试（换过 IP / 域名时用，不用重发安装包） |
| 功能开关 | 下发 `{"groupCall": false}` 之类，客户端执行；**不认识的开关会被忽略，不会报错** |
| 最低客户端版本 | 低于它就提示升级（填 `0.9.0` 这种格式，留空不限制） |
| 升级地址 | 客户端里"去升级"按钮跳的链接 |
| 公告 / 维护公告 | 客户端里展示的提示条 |

它的工作方式是：改完点「发布」→ 生成新版本 → 客户端启动时拉、每 30 分钟自动拉一次。
**只有"发布"了才生效**，编辑框里改了但没发布等于没改。

### 你这个需求里，客户端配置的唯一天然用法
  
如果你想让**所有人**都快速知道这个网络运维工具在哪，可以在「公告」里加一条：

> `网络运维工具已上线：http://192.168.31.44:5000（内网访问）`

点「发布」后，所有人的客户端上都会出现这条提示。这是"告知入口"，
和"把告警推进群"（第二节那条路）是两件独立的事，**不要指望用公告来推告警**。

---

## 七、想让卡片更好看（可选，需要动代码）

现在的效果已经可用。如果你想再进一步，有两个方向：

**A. 卡片上加"查看详情"按钮，并把设备/IP/时间拆成两列键值对**
IM 的卡片支持 `fields`（键值对，`short: true` 的会两两并排）和 `url`（底部出现「查看详情」按钮）。
工具箱的通用 Webhook 不发这两个字段，需要在中间加一层转发：工具箱 → 你的中转 → IM。
中转大概 20 行代码，做三件事：`severity → color`、把设备信息塞进 `fields`、带上 `url`。

**B. 给工具箱加一个「小智 IM」通道类型（推荐，最干净）**
工具箱的源码就在手边（v1.1 的 fpk 里 `app/src/alarm_center.py`，推送分发在 `SENDERS` 字典）。
加第 5 种通道 `"xiaozhi": _send_xiaozhi`，直接按 IM 的原生卡片格式发，
就能做到：标题带级别、设备/IP 两列排布、底部「查看详情」直连工具箱设备详情页、
@所有人可用。改完重新打个 fpk 装上即可。
（这条路的好处是以后**再也不用中间层**，且 IM 换肤/改协议时只改一处。）

---

## 附：整条链路

```
工具箱采集器（每 60 秒）
   └─ 发现离线 / 越限 ─→ 生成告警
        └─ 通知配置里的"通用 Webhook"通道
             └─ POST http://192.168.31.44:3602/api/hooks/incoming/<token>
                  └─ IM 服务端：认 token → 擦洗成卡片 → 以机器人身份落库
                       └─ WebSocket 广播
                            └─ 群里所有人收到卡片（红/橙/蓝按级别）
```

IM 服务端**只做转发和格式擦洗**，不存任何设备数据、不需要回连工具箱。
工具箱挂了 IM 不受影响；IM 挂了工具箱只是推送失败，会在自己的「推送日志」里记一笔。

# 客户端自定义模块与配置下发 — 设计规格

> 版本：v1.1（评审通过，五项待定已拍板）
> 状态：**已确认，第一期编码中**
> 目标版本：服务端 v0.8.0 / 客户端 v0.8.0

---

## 1. 要解决的问题

当前 App 的一切行为都**编译死在安装包里**。于是每次要改下面这些东西，都必须**重新打包 + 让每台设备重装**：

| 想改什么 | 现在的代价 |
|---|---|
| 服务器地址 / 外网域名 | 重发 APK（95MB），所有设备重装 |
| STUN / TURN 中继地址 | 重发 APK（本来服务端已下发 ICE，但兜底地址仍写死在包里） |
| 关掉某个出问题的功能 | 只能发新版；旧版用户完全不受控 |
| 改一段引导文案 / 公告 | 重发 APK |
| 要求老版本必须升级 | 做不到，只能靠人催 |
| 给某类用户多一个查询面板 | 发新版，且对所有人可见 |

**目标**：让服务端可以**在不重发安装包的前提下**，改变客户端的行为与界面。

**非目标**（明确不做，避免范围失控）：
- 不做"任意 App 逻辑热更新"（不下载 Dart 代码执行，Flutter 生态下这条路既违规也不稳）。
- 不做小程序式完整沙箱。
- 不改动消息收发、通话这些**核心链路的实现**——它们只受"功能开关"控制，不受动态页面影响。

---

## 2. 两层能力（已确认选择 1+3）

### A 层：配置下发（Configuration）

服务端下发一份**结构化配置**，客户端读取后改变自身行为。**不新增页面**。

覆盖内容：
1. **连接类**：候选服务器地址列表、外网域名、ICE（STUN/TURN）兜底参数
2. **开关类**：功能开关（语音消息 / 文件传输 / 通话 / 群通话 / 动态模块 / …）
3. **文案类**：登录页公告、版本提示语、引导文案、维护公告
4. **版本策略**：最低允许版本、推荐版本、强制升级阈值、升级说明与下载地址
5. **界面类**：主题色微调、默认页签、是否显示某入口

### B 层：动态渲染页面（Dynamic View）

服务端下发**页面结构 JSON**，客户端用**通用渲染器**画出来。这是真正的"长出功能模块"。

**渲染器只支持一个封闭的基础组件集**（约 8 种，见 §4）。这不是能力不足，而是刻意的约束——保证它长期可维护。

**典型用途**：
- 运营类表单：问卷、报名、意见反馈、设备报修
- 数据查看：库存概览、销售日报、值班表、公告列表
- 快捷入口：把外部系统（工厂 V2、餐饮 POS）的常用功能做成卡片入口

---

## 3. 架构与数据流

```
┌─────────────────────────────────────────────────────────────┐
│  管理台（Vue3 /admin）                                        │
│  新增「客户端配置」页：可视化编辑 + 发布 + 回滚 + 同步状态      │
└───────────────────────┬─────────────────────────────────────┘
                        │ 写库（配置以「版本」为单位，只增不改）
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  服务端                                                       │
│  ┌───────────────────────────┐   ┌────────────────────────┐ │
│  │ client_config 表           │   │ app_modules 表          │ │
│  │ (版本化配置快照)            │   │ (动态页面定义)           │ │
│  └───────────┬───────────────┘   └───────────┬────────────┘ │
│              └──────────┬────────────────────┘              │
│                         ▼                                    │
│         GET /api/client/bootstrap  ← 启动拉取（未登录可访问） │
│         GET /api/client/config     ← 已登录，含用户定向配置    │
│         WS  client:config-changed  ← 紧急变更立即推送（可选）  │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  客户端                                                       │
│  ConfigService（单例）                                        │
│   ├ 启动拉取 → 落本地缓存（shared_preferences）                │
│   ├ 每 30 分钟静默轮询 → 有变化则热生效                        │
│   ├ 版本号比对：configVersion 变了才处理                       │
│   └ 兼容性守门：minClientVersion 不满足 → 整体忽略（不崩）      │
│                                                               │
│  ModuleRenderer（通用渲染器）                                  │
│   └ stack/list/form/card/table/action/divider/text 八种组件    │
└─────────────────────────────────────────────────────────────┘
```

### 3.1 更新时机（已确认：启动拉取 + 定期轮询）

| 时机 | 行为 |
|---|---|
| App 冷启动 | 拉一次 `/api/client/bootstrap`（**未登录也能拉**，因为要拿服务器地址与维护公告） |
| 运行中 | 每 **30 分钟**静默拉一次（可配），失败静默重试，不打扰用户 |
| 从后台回到前台 | 若距上次拉取 > 5 分钟，补拉一次（手机上很常见） |
| 手动 | 「设置」页提供「检查更新」按钮 |

> **紧急变更**留了 WS 推送的扩展位（`client:config-changed`），但**首版不做**。理由：轮询兜底已经覆盖绝大多数场景，多一条推送路径就多一份"两条路状态不一致"的排障成本。等真出现"必须秒级关掉某功能"的实际需求再加。

### 3.2 生效力度（已确认：热生效）

配置拉到后**立即改变行为，不打断用户**：

| 配置项 | 热生效方式 |
|---|---|
| ICE 兜底参数 | 下次建连时读新值（`CallService` 已缓存 5 分钟，需在配置变更时清缓存） |
| 功能开关 | 直接读写，界面下一步即反映（入口显隐靠 `ValueNotifier` 重建） |
| 文案 / 公告 | 立即替换 |
| 服务器地址 | **特殊**：仅写入"候选列表"，**不打断当前连接**；下次重连或用户手动切换时才用新地址 |
| 动态页面 | 立即出现在入口菜单里 |

**同时记录配置版本**：客户端在本地存 `appliedConfigVersion`，并在「设置 → 关于」里显示，排障时能确认"这台设备吃到了哪一版配置"。诊断面板也带上这个版本号。

**生效模式（v1.1 拍板补充）**：配置项带 `applyMode: hot | restart`。
- `hot`（默认）：按上表热生效；
- `restart`：客户端写入本地"待生效区"（不立即应用），**下次冷启动时应用**；设置页提示"N 项配置将在重启后生效"。运行中绝不强制重启，避免丢失通话/未发送消息等运行态。

---

## 4. 动态页面的描述协议

### 4.1 设计原则

1. **封闭组件集**：只支持 8 种组件，服务端不能发明新组件名（未知组件 → 客户端渲染成"暂不支持"占位块，不崩）。
2. **数据与结构分离**：页面结构（schema）由服务端下发；页面数据由动态页面自己调**白名单接口**拉取。
3. **动作白名单**：动态页面只能触发预先注册好的动作（见 §4.4），**不能访问任意 URL**。
4. **向下兼容**：客户端遇到看不懂的字段一律忽略，不报错。

### 4.2 顶层结构

```json
{
  "moduleId": "stock_overview",
  "title": "库存概览",
  "icon": "inventory",
  "configVersion": 12,
  "minClientVersion": "0.8.0",
  "visibleTo": { "roles": ["admin", "user"], "userIds": [] },
  "body": [ /* 组件数组，见 §4.3 */ ],
  "onLoad": { "action": "api", "path": "/api/hooks/stock/overview", "method": "GET" }
}
```

| 字段 | 说明 |
|---|---|
| `moduleId` | 唯一标识，客户端据此做缓存与去重 |
| `minClientVersion` | 客户端版本低于此值 → **整体忽略该模块**（不显示、不崩） |
| `visibleTo` | 可见范围：按角色 / 指定用户；首版不支持按群组（见 §8 待定项） |
| `onLoad` | 页面打开时执行的取数动作，结果注入 `data` 供组件绑定 |

### 4.3 组件集（8 种）

| 组件 | 用途 | 关键字段 |
|---|---|---|
| `text` | 展示一段文字 | `text`, `size`, `color`, `align` |
| `divider` | 分隔线 | — |
| `card` | 卡片容器（可含子组件） | `title`, `subtitle`, `children[]`, `accent` |
| `list` | 列表（动态数据） | `dataPath`, `itemTemplate{title,subtitle,trailing}` |
| `table` | 表格（动态数据） | `dataPath`, `columns[{key,label,width}]` |
| `form` | 表单（可提交） | `fields[{key,label,type,required,options}]`, `submit{action}` |
| `action` | 按钮 | `label`, `onTap{action}` |
| `chart` | 简易柱状/折线 | `dataPath`, `xKey`, `yKey`, `kind` |

**字段类型**（`form` 内）：`text` / `number` / `select` / `date` / `textarea` / `switch`

**颜色规则（v1.1 拍板补充）**：组件的 `color`/`accent` 只允许**语义枚举**（`primary` / `muted` / `danger` / `success` / `warning`），**不允许具体色值**——客户端按当前主题（亮/暗）映射到主题令牌，保证深色主题自适应（拍板项 3）。

**为什么是这 8 种**：覆盖"看数据、录数据、点按钮"三类运营动作；每种都能用 Flutter 原生组件一次写好、长期不动。加第 9 种之前必须先证明 8 种确实表达不了。

### 4.4 动作白名单

动态页面**唯一**能做的事就是触发这些动作：

| action | 说明 | 安全约束 |
|---|---|---|
| `api` | 调一个后端接口 | `path` 必须以 `/api/hooks/` 开头（见下） |
| `navigate` | 跳到客户端**内置**页面 | `page` 必须在预置枚举内（会话 / 通讯录 / 设置 / 收藏 …） |
| `copy` | 复制文本到剪贴板 | — |
| `openUrl` | 外部浏览器打开 | **必须 https**，且需用户在服务端配置过域名白名单 |
| `submit` | 提交表单 | 走 `api` 同一套约束 |

**`/api/hooks/*` 是关键设计**：服务端提供一个**通用受控转发**路由，只允许访问在管理台登记过的上游地址。这样动态页面能对接工厂 V2 / 餐饮 POS，但**不能**变成任意 URL 的跳板（否则就是一个 SSRF 漏洞）。

### 4.5 一个完整示例

```json
{
  "moduleId": "device_repair",
  "title": "设备报修",
  "icon": "build",
  "configVersion": 3,
  "minClientVersion": "0.8.0",
  "body": [
    { "component": "text", "text": "提交后维修班会收到通知", "size": 13, "color": "muted" },
    { "component": "form",
      "fields": [
        { "key": "device", "label": "设备名称", "type": "text", "required": true },
        { "key": "level", "label": "紧急程度", "type": "select", "required": true,
          "options": [{"label":"一般","value":"low"},{"label":"紧急","value":"high"}] },
        { "key": "desc", "label": "问题描述", "type": "textarea" }
      ],
      "submit": { "action": "api", "method": "POST", "path": "/api/hooks/repair/create" }
    }
  ]
}
```

---

## 5. 数据模型（新增表）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `client_configs` | id, version(自增), payload(JSON), note, created_by, created_at | **版本化快照，只增不改**。回滚 = 把旧版重新发布为新版本 |
| `app_modules` | id, module_id, title, icon, schema(JSON), min_client_version, enabled, sort, visible_roles, visible_user_ids, updated_at | 动态页面定义 |
| `hook_endpoints` | id, name, url, method, headers(JSON), enabled, created_at | `/api/hooks/*` 的上游白名单 |
| `config_applied` | user_id, device_id, config_version, applied_at | 哪台设备吃到了哪版（排障用） |

> **回滚语义**：`client_configs` 只增不改，所以"回滚到 v5"实际是"用 v5 的内容发行 v13"。好处是历史永远可查，坏处是版本号会涨——这是刻意的取舍。

---

## 6. 服务端接口

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/client/bootstrap` | **免鉴权** | 服务器地址候选、维护公告、最低版本策略、功能开关(**不含用户定向**)、动态模块列表(仅 `visibleTo` 为 all 的) |
| GET | `/api/client/config` | 需登录 | 完整配置，含按用户/角色定向的部分 |
| POST | `/api/client/report-applied` | 需登录 | 客户端上报已生效版本（写 `config_applied`） |
| GET | `/api/hooks/:name` | 需登录 | 受控转发（白名单） |
| POST | `/api/hooks/:name` | 需登录 | 受控转发（白名单） |
| GET/POST | `/api/admin/client-config` | admin | 管理台：读当前、发布新版、查看历史 |
| POST | `/api/admin/client-config/rollback` | admin | 回滚（= 发布旧内容为新版） |
| GET/POST/PUT/DELETE | `/api/admin/modules` | admin | 动态页面 CRUD |
| GET/POST/PUT/DELETE | `/api/admin/hooks` | admin | 上游白名单 CRUD |

### 6.1 bootstrap 免鉴权的取舍

**为什么免鉴权**：App 冷启动时可能还没登录（甚至可能连不上主地址、正要切换备用地址），此时**恰恰最需要拿到配置**。如果要求鉴权，就变成了"连不上 → 拿不到配置 → 永远连不上"的死锁。

**代价**：bootstrap 的内容对未授权者可见。**所以 bootstrap 里绝不能放任何敏感信息**：

- ✅ 可以放：地址候选、公告、版本策略、公开的功能开关、公开动态模块
- ❌ 不能放：token、密钥、用户数据、内部地址、按用户定向的配置

`/api/client/config`（需登录）才返回定向内容。

### 6.2 版本协商（防崩的关键）

客户端拿到配置后按此顺序校验：

```
1. 配置自带 configVersion；与本地 appliedConfigVersion 相同 → 什么都不做（最常见路径）
2. 解析 payload；解析失败 → 保留旧配置，静默放弃（绝不崩、绝不清空）
3. 校验 minClientVersion：本机版本 < 要求 → 整体忽略这份配置
   （因为新配置可能依赖新客户端才认识的能力）
4. 逐个处理开关/文案/模块；单个条目出错 → 跳过该条，继续处理其余
5. 全部成功 → 落缓存、更新 appliedConfigVersion、热生效
```

**"静默放弃"是刻意的**：配置下发是"锦上添花"的能力，它出问题**绝不能导致 App 不可用**。任何异常路径都以"保持现状"收场。

---

## 7. 管理台（新增「客户端配置」页）

按你选的"可视化编辑"，页面分三个标签页：

### 7.1 配置（Configuration）
- 表单化编辑各配置项，**带说明文字**（每项下面写清"改了会影响什么"）
- 右侧显示 **当前线上版本 / 上次发布时间 / 发布人**
- 「发布」前弹**变更对比**（这次改了哪几项，从什么改到什么）——防止手滑
- **历史版本列表**，任意版本可「回滚」（= 以旧内容发新版）

### 7.2 动态模块（Modules）
- 模块列表（可拖拽排序、启用/停用）
- 编辑器：**左侧 JSON、右侧实时预览**（预览用 Element Plus 近似渲染，不追求和 Flutter 像素一致，但结构一目了然）
- 内置**模板库**（表单 / 列表 / 表格 / 卡片几个常用骨架），从模板起步，不用从空白 JSON 写

### 7.3 上游接口（Hooks）
- 白名单登记：别名、URL、方法、需要的请求头
- **「测试」按钮**：当场发一次请求，显示返回结果——避免配错了到客户端才发现

### 7.4 同步状态
- 显示哪些用户/设备已上报吃到了哪个版本
- 一眼能看出"有 3 台设备还在旧配置"（这是排障时最想知道的事）

---

## 8. 安全边界

这部分是**必须守住**的，否则这个功能会变成攻击面：

| 风险 | 对策 |
|---|---|
| 动态页面变成任意 URL 跳板（SSRF） | 只能走 `/api/hooks/:name`，上游地址由管理台登记，**不接受页面传入完整 URL** |
| `openUrl` 打开钓鱼站 | 强制 https + 服务端配置域名白名单 |
| 配置内容被中间人篡改 | 走 HTTPS；**可选**加 HMAC 签名（`X-Config-Signature`），服务端与客户端共享一个配置密钥 |
| 恶意配置导致 App 崩溃 | §6.2 的协商流程：任何异常都保持现状 |
| 配置泄露内部信息 | bootstrap 免鉴权，因此严格限定其内容（§6.1） |
| 动态表单提交越权数据 | `/api/hooks/*` 在**服务端**校验字段与权限，不信任客户端的表单定义 |

---

## 9. 分期计划

严格按顺序做，每期可独立验收、独立上线。

### 第一期：配置下发骨架（**建议先做这个**）
- 服务端：`client_configs` 表 + bootstrap/config/report-applied 接口 + 管理台「配置」页
- 客户端：`ConfigService`（拉取 / 缓存 / 版本比对 / 热生效）+ 「关于」显示配置版本
- **先接 3 个高价值配置项**验证链路：① 服务器地址候选 ② 功能开关 ③ 版本策略（最低版本提示）
- **验收**：改一次服务器地址，App **不重装**就能用新地址连接

### 第二期：动态模块渲染器
- 服务端：`app_modules` 表 + 管理台「动态模块」页（含模板库）+ 菜单下发
- 客户端：`ModuleRenderer` + 8 种组件，**全部走主题令牌实现深色自适应**（拍板项 3）
- **验收**：在管理台拼一个"设备报修"表单，App 端**不重装**就出现入口并能提交

### 第三期：对接外部系统
- 服务端：`hook_endpoints` 表 + `/api/hooks/*` 受控转发 + 管理台「上游接口」页
- 页面数据全部**服务端算好**后下发（拍板项 1），客户端只渲染
- **验收**：动态页面能拉到工厂 V2 的真实数据并渲染成列表

### 第四期（按需）
- 分环境/分组下发、WS 紧急推送、`chart` 组件
- ~~配置签名~~（拍板项 4：不做）
- 群组可见性（拍板项 2 的后期跟进部分，可并入此期或第五期）

### 第五期：客户端自更新（拍板项 5/6，新增）
- **restart 型配置**：配置项带 `applyMode: hot | restart`，restart 项写入"待生效"区，冷启动时应用；设置页提示"N 项配置将在重启后生效"
- **客户端自更新**：服务端发布新客户端包（APK/Windows zip）到管理台 → 客户端轮询发现新版本 → **App 内下载更新包并自动应用**，用户无需重新下载安装：
  - Android：下载 APK 到应用缓存 → 弹安装确认（系统限制，无法完全静默，但全程在 App 内引导）
  - Windows：下载新版本文件集 → 校验 md5 → 重启进程前替换（runner 与 DLL 用临时目录交换方案）
  - 全程断点续传 + md5 校验，失败自动回退当前版本
- **验收**：服务端发布一次新版本，两台设备在 App 内完成升级，全程不碰浏览器/文件管理器

---

## 10. 待定项 — 已全部拍板（2026-09-12 龙哥确认）

1. **动态页面数据由谁算？** → ✅ **服务端算好**。服务端给什么客户端就显示什么，客户端不做任何计算逻辑。
2. **是否按群组下发？** → ✅ **要，但后期跟进**。首版仍按角色/用户下发；群组可见性作为独立增强排入后期（见 §9 第五期）。
3. **深色主题自适应？** → ✅ **要做**。动态页面组件全部走客户端主题令牌（背景/文字/强调色跟随当前主题），不允许 JSON 里写死颜色值（`color` 只允许 `primary/muted/danger` 等语义枚举，不允许具体色值）。
4. **配置签名？** → ✅ **不做**。安装包内置基础功能、其余靠下发，签名防不住"有安装包的人"，收益有限。安全靠 HTTPS + 白名单机制保障（§8）。
5. **"配置生效需要重启"的类型？** → ✅ **要引入**，且语义扩展为两类：
   - `hot`（默认）：热生效，不打断用户；
   - `restart`：写入"待生效"区，**下次冷启动时应用**（不做运行中强制重启，避免丢状态）。客户端在设置页明确提示"有 N 项配置将在重启后生效"。
6. **客户端自更新（新增需求）** → ✅ **要做**：服务端后续更新主文件后，若需要更新客户端，**直接向客户端推送更新包**，客户端在 App 内下载并应用，**用户不需要重新下载安装**。详见 §9 第五期。

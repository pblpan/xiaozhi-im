# 远程协助（对标 UU 远程）— 需求与方案草稿

> **状态：第一期 + 第二期 + 第四期（部分）已交付 —— 服务端 v0.10.0 / 客户端 v0.9.1（2026-09-13）。**
> 龙哥 2026-09-13 提出：「把 UU 远程的功能集合到软件里，**在完成计划任务后执行**」。
> 排期：跟在「动态配置与模块」系列（`SPEC-动态配置与模块.md`）之后，第二期动态模块
> （`19566b8`）落地后开工。参照物：`D:\Program Files\Netease\GameViewer`（UU 远程，
> 闭源，只能对标功能、不能复用代码）。
>
> ⚠️ 本文档 §1~§5 保留为**原始草稿**（含当初的待确认问题），实际取舍与落地情况见文末
> §6 —— 先读 §6 再看草稿，否则会被草稿里"先做只看版本"的保守建议误导。

---

## 1. 想要什么（我的理解，待确认）

在**小智 IM 内部**发起远程协助：聊天窗口里点一下「请求远程协助」→ 对方同意 → 我看到他的屏幕、能控制他的键鼠。

典型场景（贴合龙哥实际）：
- 门店/工厂同事电脑出问题，你远程过去帮他弄，不用再装一个 UU 远程
- 被协助方不需要懂配置，点"允许"就行（降低门槛是核心价值）

### 需要龙哥确认的范围（决定工作量）

| # | 问题 | 选项 | 影响 |
|---|---|---|---|
| 1 | 只要**看**（屏幕共享）还是要**控**（键鼠）？ | 看 + 控 / 只看 | 「控」要写键鼠注入，工作量翻倍且是安全高危点 |
| 2 | 平台范围？ | Windows→Windows 优先 / 含 Android | Android 被控端基本做不到（系统限制），通常只能 Android 看/控 Windows |
| 3 | 是否要**无人值守**（固定密码，人不在也能连）？ | 要 / 不要（必须对方在场点同意） | 无人值守 = 常驻服务 + 长期凭据，风险显著上升 |
| 4 | 是否要穿透现有 TURN？ | 复用 / 单独 | 复用可直接吃到 Cloudflare + coturn 双保险 |

---

## 2. 技术路线（初步判断）

**复用已有基建是最大优势**——小智 IM 已经跑通的东西能直接拿来用：

| 能力 | 现状 | 远程协助怎么用 |
|---|---|---|
| 信令通道 | WebSocket 已稳定（含定向投递 `to`） | 复用，新增 `remote:*` 消息族 |
| P2P 打洞 + 中继 | flutter_webrtc + Cloudflare TURN + coturn | **直接用**，跨网不用再配一次 |
| 呼叫流程 | v0.7.0 多方通话的房间/参与者模型 | 远程协助本质是「1v1 呼叫 + 一条屏幕轨 + 一条控制通道」 |
| 客户端 | Flutter（Windows + Android） | Windows 端可控可看；Android 端先看 |

### 新增的三块

1. **屏幕采集（被控端，Windows）**
   - 候选 A：`flutter_webrtc` 的 desktop capture（`getDisplayMedia`）—— 与现有 WebRTC 一套，改动最小
   - 候选 B：`screen_capturer` / `webrtc` 原生插件 —— 备选
   - 多显示器选择、帧率/清晰度档位（网络差时降档）

2. **键鼠注入（被控端，Windows）**
   - 通过 DataChannel 接收事件 → Windows `SendInput` 注入
   - 需要写 **FFI/plugin**：Dart 侧没有现成的 `SendInput` 绑定，要加 Windows 插件（或用 `win32` 包）
   - 坐标换算：控制端看到的画面尺寸 ≠ 被控端实际分辨率，必须按实际分辨率映射

3. **控制通道**
   - `RTCDataChannel`（可靠 + 有序）传键鼠事件；比走服务端转发延迟低得多
   - 事件协议要**版本化**（`v:1`），为将来加剪贴板/文件传输留口

---

## 3. 安全边界（这部分不能省）

远程协助是**高权限能力**，做得不严谨会变成远控木马。必须守住：

| 风险 | 对策（初步） |
|---|---|
| 悄悄被控 | **必须**对方明确点"同意"才开始；会话期间被控端有**醒目常驻提示条**（含"立即断开"） |
| 无人值守被滥用 | 若做无人值守：独立开关（默认关）+ 一次性/可轮换的访问码 + 服务端记录每次会话 |
| 控制指令伪造 | DataChannel 建在已鉴权的 P2P 上；被控端校验会话状态，非活跃会话的指令一律丢弃 |
| 隐私泄露 | 控制端可看时，被控端应能一键暂停共享；密码框类窗口考虑屏蔽（后期） |
| 审计缺失 | 每次会话写 `remote_sessions`（谁、何时、时长、谁发起） |

**倾向建议**：先做「必须对方在场点同意」的**有人值守**版本，跑顺了再考虑无人值守。

---

## 4. 分期草案（等确认后再定稿）

1. **第一期**：Windows→Windows **只看**（屏幕共享），复用通话信令与 TURN，验证跨网能看
2. **第二期**：加**键鼠控制**（SendInput 插件 + 坐标换算 + DataChannel 事件协议 v1）
3. **第三期**：体验补齐（清晰度/帧率自适应、全屏、剪贴板同步、会话审计与记录）
4. **第四期（按需）**：无人值守访问码、Android 端观看、文件传输

---

## 5. 开工前必须确认的前置

- [ ] 上面 §1 的 4 个问题拍板
- [ ] 确认 Windows 端屏幕采集方案能跑通（**先做一个 30 行的技术验证**：起个 P2P 把桌面推给自己看）
- [ ] `SendInput` 注入方案验证（能否用 `win32` Dart 包直接做，还是必须写 plugin）

> 技术验证结论会直接改写这份文档的分期与工作量估计。

---

## 6. v0.10.0 实际交付情况（草稿的答复）

### 6.1 §1 那 4 个问题的最终取舍

当初留的 4 个待确认项，实际是按"一次做够"的方向落的 —— 原因是这些能力互相咬合，
拆开做反而要多付一遍接入成本：

| # | 草稿里的选项 | 实际取舍 | 理由 |
|---|---|---|---|
| 1 | 只看 / 看+控 | **看 + 控** | 只看解决不了实际问题（看到卡住了还得打电话指挥按键）。「控」的安全代价是有办法管的，见 §6.3 |
| 2 | Windows→Windows / 含 Android | **Windows 优先；Android 可观看** | Android **作为被控端**：可以看它的屏幕，但键鼠注入至今没做（系统限制，要原生无障碍服务）。这里刻意**没有假装支持**——详见 §6.4 |
| 3 | 无人值守要 / 不要 | **要，但加了 10 秒反悔窗口** | 纯有人值守在"同事已经下班"的场景直接没用。有码也不立刻开控，给窗口期让他反悔，是安全与可用的折中 |
| 4 | 复用 / 单独穿透 | **复用现有 TURN** | 直接吃到 Cloudflare + coturn 双保险的成熟结果，不用重配一次跨网 |

### 6.2 实现落在哪几个文件

| 位置 | 职责 |
|---|---|
| `server/src/remote.js` | 会话状态机 + 三条硬边界（1v1、只转发给会话内另一个人、掉线即拆）+ 访问码管理 |
| `server/src/routes/remote.js` | 访问码 CRUD + 会话审计查询（**不含任何画面/键鼠数据**） |
| `server/src/ws.js` | `remote:*` 信令路由，每条失败都回 `error`（远程协助不能让人盯着转圈） |
| `server/src/db.js` | `remote_sessions` / `remote_access_codes` / `remote_code_attempts` |
| `client/lib/core/remote_assist.dart` | WebRTC 屏幕轨 + `control` DataChannel + 控制协议解析 |
| `client/lib/core/input_inject.dart` | 键鼠注入抽象层（Windows 走 Win32 `SendInput`） |
| `client/lib/screens/remote.dart` | 主入口 / 访问码 / 历史 + 控制挡板 + 全局 watcher |

### 6.3 草稿里的风险对策，实际是怎么落实的

| 草稿风险 | 落地措施 | 有没有硬证据（谁都能跑出来） |
|---|---|---|
| 悄悄被控 | 被控端同意之前，服务端**不转发任何 SDP/ICE**——不是"先建好连接再等同意"，是真的没建立 | `server/test/remote_e2e.js`：「还没同意就发 offer：服务端回绝发起方」+「被控端收不到 SDP」两条。**做过变异测试**：把这层拦截删掉，这两条会真的变红并打印出服务端放了什么过来 |
| 控制指令伪造 | 客户端红线：`!isHost \|\| phase != active` 一律丢弃；服务端的水 friendlyًا层在上一条 | `client/test/remote_input_test.dart` 的畸形/恶意输入分组（对端发 `{"x":"0.5"}` 也不会崩） |
| 无人值守被滥用 | 访问码只存 **scrypt** 慢哈希（不是可被离线枚举的摘要，9 位码空间只有 10⁹）；连撞 10 次锁定；一次性码用过即废 | `server/test/remote_e2e.js`：重算 `sha256(salt+code)` 与实际存储值比对，相等即判定防护失效 |
| 审计缺失 | 每次会话记谁控了谁、时长、结束原因；任一方掉线立即拆；单次会话硬上限 4 小时 | `server/test/remote_e2e.js` 第 ⑩ 组：掉线后对端收到结束 |

### 6.4 明确知道自己没做到的（不许含糊）

- **Android 作为被控端不能被操作**。不是"不知道行不行"，是**原生无障碍服务还没写**。
  `AndroidInputInjector.supported` 因此恒定返回 `false`，同意弹窗会如实打印出来。
  若照 `Platform.isAndroid` 报 true，用户会对着一块毫无反应的屏幕反复点——
  **假装能控比直接说不支持糟糕得多**。等原生侧补齐（MainActivity 的 MethodChannel
  + AccessibilityService + `dispatchGesture`）再把它换成异步探测。
- **多显示器只取「整个虚拟桌面」归一化**：若被控端有多块屏而共享的只是其中一块，
  控制端画面里的 0..1 会映射到整块虚拟桌面而非那块屏。要修正得先让采集侧回报
  「共享的是哪一块」，`getDisplayMedia` 目前拿不到。
- 第三期里列的清晰度/帧率自适应、剪贴板同步、文件传输都没做。

### 6.5 过程中踩到并修掉的坑（给后来人）

1. **写了的功能没人接线**：`remote.dart` 最初没有任何地方导航过去，服务也从不
   `start()`——等于整个功能不存在。是"编译通过分析也通过、跑起来什么都没有"。
   现在挂在 `MaterialApp.builder`（包任何单条路由都会在 push 之后盖不住）。
2. **` aberrância 病例`：绝对 mouse 坐标算错**：原先"先加虚拟屏原点、再除屏幕总宽"，
   副屏摆在主屏左侧时算出负坐标。实际上 `MOUSEEVENTF_ABSOLUTE | VIRTUALDESK`
   的语义就是 0..65535 覆盖整个虚拟桌面，直接乘就行。
3. **Ctrl/Shift/Alt 全组漏映射**：表象不是报错，是"对方电脑上按不出 Ctrl+C"。
   这类问题靠联调极难发现（鼠标和打字都正常），所以把 `_keyOf` 抽成了纯函数单测。
4. **TURN 向导的测试有 7 条断言是 v0.8.0 改行为后没跟着改的**（还在断言老版的
   "需要重建容器"），而且里面硬编码了**真实 Key ID 的前四位**。详情见 commit。
